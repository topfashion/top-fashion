export default async function handler(req, res) {
  if (req.method !== 'POST') {
    return res.status(200).send('OK');
  }

  const body = req.body;
  if (!body || !body.message) {
    return res.status(200).send('OK');
  }

  const chatId = body.message.chat.id;
  const userText = body.message.text;

  if (!userText) {
    return res.status(200).send('OK');
  }

  const botToken = process.env.TELEGRAM_BOT_TOKEN;
  const openRouterApiKey = process.env.GEMINI_API_KEY;

  // 1. Привет! Я умный помощник школы-задайте мне вопрос (команда /start):
  if (userText === '/start') {
    const welcomeMessage = "Привет! Я умный помощник детской модельной школы развития. Задайте мне любой вопрос о направлениях обучения, расписании, ценах или записи на пробное занятие.";
    await sendTelegramMessage(chatId, welcomeMessage, botToken);
    return res.status(200).send('OK');
  }

  try {
    const aiResponse = await getOpenRouterResponse(userText, openRouterApiKey);
    await sendTelegramMessage(chatId, aiResponse, botToken);
  } catch (error) {
    console.error(error);
    await sendTelegramMessage(chatId, "Извините, произошла техническая ошибка. Пожалуйста, попробуйте написать позже или свяжитесь с администратором школы.", botToken);
  }

  return res.status(200).send('OK');
}

async function getOpenRouterResponse(prompt, apiKey) {
  // 2. ИНСТРУКЦИИ ДЛЯ ИИ И БАЗА ЗНАНИЙ (редактируйте текст ниже):
  const systemInstruction = `Вы — вежливый, заботливый и профессиональный администратор детской модельной школы развития "ТопФешен". 
Ваша задача — отвечать на вопросы родителей и учеников.

ПРАВИЛА ОБЩЕНИЯ (СТРОГИЕ):
1. Не здоровайтесь в каждом сообщении! Приветствие уже было отправлено пользователю при старте. Начинайте ответ сразу с сути вопроса. Исключение — если пользователь сам поздоровался в этом сообщении.
2. Не пишите фразы вроде "Рад помочь!", "Здравствуйте!", "Приветствую!" в начале ответа. Пишите сразу ответ на вопрос.
3. Разрешено использовать творческую эрудицию: если вас спрашивают об общих понятиях моделинга (например, "что такое фототренинг?", "что надеть на фотосессию в черном стиле?", "как подготовить ребенка к первой съемке?"), дайте развернутый, профессиональный и полезный совет по стилю и позированию, используя свои знания.
4. Конкретные данные школы (цены, адреса, контакты) берите строго из базы знаний ниже. Не придумывайте свои цены или расписание! Если вас спрашивают о конкретных деталях школы, которых нет в базе знаний, вежливо предложите написать администратору.

База знаний модельной школы:
- ТопФешенСервис-это агенство которое 13 лет работает в Сургуте и охватывает широкий спектр интеллектуальной и творческой деятельности. Мы организуем мероприятия своего агенства и города, у нас есть база моделей, сертифицированная школа развития для детей и курсы для взрослых. 
- Возраст детей: принимаем мальчиков и девочек от 4 до 25 лет. Группы делятся по возрастам: младшая (4-7 лет), средняя (8-12 лет), средняя (13-17 лет), старшая (17-25 лет). Есть курсы от Елены Полеевой для взрослых и разные МК и курсы от преподоветелей.
- Направления обучения: походка, фотопозирование (с практикой на съемочной площадке), фототренинг, актерское мастерство, основы стиля, этикет, основы моделинга, ораторское искусство, психология, ежемесечные фотосессия, пластика и осанка.
- Основной состав моделей: юноши и девушки-которые прошли кастинг и обучение и участвуют в показах и фотосессиях.
- Закрытый женский клуб "Стильная среда" для выпускниц образовательного проекта "Лучшая версия себя" под руководством Инны Ашихминой-психолог и высококлассный стилист с большым порфолио известным в Сургуте.
- Интеллект-клуб "Умные люди" под руководством Елены Саган-преподает ораторское искусство, потому что ее голос и поставленную речь слышал каждый житель нашего города.
- Летний модельный лагерь в Сургуте.
- Конкурс "Мисс Сургут"
- Женские группы 30+ Елены Полеевой-главная муза проекта и та, которую вы видели на крупных мероприятиях города.
- Стоимость обучения: (1 месяц- 6.500 руб., разовое занятие- 600 руб. занятия 2 раза в неделю по 1.5 часа и отчетная фотосессия ежемесечная.
- Разовое занятие: дает возможность посетить одно занятие с желаемой группой, не закрепляет место в группе
- Форма на занятия: однотонная черная облегающая форма (лосины/джоггеры; футболка/топ; черные носки и сменная обувь-группы до 12 лет на твердой подошве(лоферы/школьные туфли) группы от 12 лет лодочки на каблуке.
- Записаться можно через наше приложение/соц.сети/телефон.
- Как записаться через приложение- авторизируйтесь под своим номером и введите данные, в нижней панели меню вкладка "Магазин"- выберите и купите абонимент. Во вкладке "Расписание"-внесение в нужную группу и вы в списке! и потом с Вами свяжется наш админ.
- Адрес: ул. Университетская д. 11 2 этаж-вход и заезд со стороны дороги (поворот напротив Бристоля) крыльцо клиники Молекула-напротив ресторана Даги и Греки.
- Контакты: телефон +7 (912) 817-99-99, есть тг и макс. @TopFashionService
- Есть свое приложение с расписанием и доп. информацией https://apps.apple.com/ru/app/tfs-agency/id6744437569
- Частые вопросы: Родителям присутствовать на занятиях нельзя, чтобы дети не отвлекались. В конце каждого курса мы проводим отчетный показ и выдаем фирменный сертификат.В конце каждого месяца проводим фотосессии.`;

  // Запрос отправляется к стабильной премиум-модели Llama 3.1 от Meta
  const response = await fetch("https://openrouter.ai/api/v1/chat/completions", {
    method: "POST",
    headers: {
      "Authorization": `Bearer ${apiKey}`,
      "Content-Type": "application/json"
    },
    body: JSON.stringify({
      model: "meta-llama/llama-3.1-8b-instruct",
      messages: [
        { role: "system", content: systemInstruction },
        { role: "user", content: prompt }
      ]
    })
  });

  const data = await response.json();
  
  console.error("ОТВЕТ ОТ LLAМА:", JSON.stringify(data, null, 2));

  if (data.choices && data.choices[0] && data.choices[0].message && data.choices[0].message.content) {
    return data.choices[0].message.content;
  }
  throw new Error('Invalid OpenRouter API response');
}

async function sendTelegramMessage(chatId, text, token) {
  const url = `https://api.telegram.org/bot${token}/sendMessage`;
  await fetch(url, {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json'
    },
    body: JSON.stringify({
      chat_id: chatId,
      text: text
    })
  });
}
